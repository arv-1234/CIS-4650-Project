/* Test 3: Exhibit different kinds of semantic errors (but no more than 3 per program)

  Error 1: line 13, i can't be redefined as type bool.
  Error 2: line 14, j can't be defined as type void.
  Error 3: line 31, k does not exist outside of its nested scope/block.

*/

int x[10];

int main(void) {
  int i;
  bool i;
  void j;

  i = 0;

  while (i < 10) {
    x[i] = input();
    i = i + 1;
  }

  sort(x, 0, 10);
  
  {
    int k;
    {
      k = 1;
    }
  }
  k = 2;

  return 0;
}
